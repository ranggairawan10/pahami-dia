// ============================================================
// Onboarding Layout Override
// Onboarding tampil full-screen tanpa app shell.
// Override layout dari (app)/layout.tsx untuk route ini saja.
// ============================================================

export default function OnboardingLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
